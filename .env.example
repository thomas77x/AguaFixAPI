PORT=3000
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=change_me
DB_NAME=aguafix

# SMTP local de prueba (por ejemplo, Mailpit). Para correo real usa tu proveedor.
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_SECURE=false
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=aguafix@example.test
MAINTENANCE_EMAIL=cuadrilla@example.test
